<Window x:Class="XPSUI.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XPSUI"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="400">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Close"/>
        </Menu>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Shift Peak Center:" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="Shift_Peak_Center" Height="25" Margin="0,0,0,10" Text="284.4"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="X Max" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="X_Max" Height="25" Margin="0,0,0,10" Text="290.0"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="X min" VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="X_min" Height="25" Margin="0,0,0,10" Text="280.0"/>
            <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="Save Settings" Height="30" Width="100" Margin="0,20,0,0" Click="SaveButton_Click" />

        </Grid>
    </DockPanel>
</Window>
